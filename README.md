# hass-energy

Base uv-managed CLI skeleton for the `hass-energy` tool. The CLI is powered by Click and ready for additional modes (e.g., daemonised run, training, etc.).

## Getting started

```bash
uv sync
uv run hass-energy --help
uv run hass-energy hello
```
